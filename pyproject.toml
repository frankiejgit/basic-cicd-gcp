[project]
name = "basic-cicd-gcp"
version = "0.1.0"
description = "Basic CI/CD for GCP projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
]
